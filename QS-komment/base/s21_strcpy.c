#include "../s21_string.h"

/*

Функции `strncat()` и `strcpy()` (или ваша версия `s21_strcpy()`) в языке C служат для разных целей при работе со строками. Давайте рассмотрим их различия:

### `strncat()`
`strncat()` используется для конкатенации (добавления) одной строки к другой, ограничивая количество добавляемых символов.

#### Прототип:
```c
char *strncat(char *dest, const char *src, size_t n);
```

#### Параметры:
- `dest`: указатель на строку назначения, к которой будут добавлены символы.
- `src`: указатель на строку-источник, из которой будут добавлены символы.
- `n`: максимальное количество символов для добавления из строки-источника.

#### Возвращаемое значение:
- Указатель на строку назначения `dest`.

#### Особенности:
1. **Работа со строками**: предназначена для работы со строками, завершающимися символом `\0`.
2. **Символ окончания строки**: добавляет символ окончания строки `\0` в конец результирующей строки.
3. **Ограничение длины**: добавляет не более `n` символов из строки-источника.

#### Пример:
```c
#include <stdio.h>
#include <string.h>

int main() {
    char dest[20] = "Hello, ";
    char src[] = "world!";
    strncat(dest, src, 3); // Добавит только первые 3 символа из src

    printf("%s\n", dest); // Output: "Hello, wor"

    return 0;
}
```

### `s21_strcpy()`
`strcpy()` (или ваша версия `s21_strcpy()`) используется для копирования строки, включая символ окончания строки `\0`, из одного места в другое.

#### Прототип:
```c
char *s21_strcpy(char *dest, const char *src);
```

#### Параметры:
- `dest`: указатель на строку назначения, куда будет копироваться строка.
- `src`: указатель на строку-источник, которая будет скопирована.

#### Возвращаемое значение:
- Указатель на строку назначения `dest`.

#### Особенности:
1. **Полное копирование строки**: копирует всю строку, включая символ окончания строки `\0`.
2. **Перезапись**: перезаписывает содержимое строки назначения полностью.

#### Пример:
```c
#include <stdio.h>
#include <string.h>

char *s21_strcpy(char *dest, const char *src) {
    char *ptr = dest;
    while (*src) {
        *ptr++ = *src++;
    }
    *ptr = '\0';
    return dest;
}

int main() {
    char dest[20];
    char src[] = "Hello, world!";
    s21_strcpy(dest, src);

    printf("%s\n", dest); // Output: "Hello, world!"

    return 0;
}
```

### Основные различия

1. **Назначение**:
   - `strncat()` предназначена для конкатенации строк, добавляя символы из одной строки в конец другой.
   - `s21_strcpy()` предназначена для копирования строки из одного места в другое, полностью заменяя содержимое строки назначения.

2. **Символ окончания строки**:
   - `strncat()` добавляет символ окончания строки `\0` в конец результирующей строки после конкатенации.
   - `s21_strcpy()` копирует символ окончания строки `\0` из исходной строки в строку назначения.

3. **Ограничение длины**:
   - `strncat()` ограничивает количество добавляемых символов из строки-источника, чтобы не превышать `n` символов.
   - `s21_strcpy()` копирует всю строку, включая символ `\0`, не ограничивая количество символов.

4. **Перезапись**:
   - `strncat()` добавляет символы в конец строки назначения, сохраняя ее существующее содержимое.
   - `s21_strcpy()` перезаписывает содержимое строки назначения полностью.

### Применение

- **`strncat()`**: используется для добавления одной строки к другой, ограничивая количество добавляемых символов.
- **`s21_strcpy()`**: используется для полного копирования одной строки в другую, заменяя все ее содержимое.

Эти различия делают каждую функцию полезной для своих конкретных задач при работе со строками в языке C.


*/



char *s21_strcpy(char *dest, const char *src) {
  for (int i = 0; src[i]; ++i) dest[i] = src[i];
  dest[s21_strlen(src)] = '\0';
  return dest;
}
